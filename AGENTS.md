# Codinal agent policy

Read the SSOT: [harness/AGENTS.md](harness/AGENTS.md).

Minimal agent configuration (models, plugins, MCP):
[harness/config/agent.yaml](harness/config/agent.yaml).

To switch models across all supported providers, change `active_model` in
`harness/config/agent.yaml` and run `harness agent check`.

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "No privileged frontend capabilities are enabled in Phase 1.3.",
  "windows": ["main"],
  "permissions": []
}

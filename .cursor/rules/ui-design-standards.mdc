---
description: General writing, typography, and copywriting standards
alwaysApply: true
---

# General Writing & Typography Standards

## 1. Typography
- **Em Dash**: Always use the correct em dash symbol (`—`). Never use double hyphens (`--`) or single hyphens (`-`) for dashed pauses in text/documentation.

## 2. Professional Tone
- **Tone**: Maintain a highly objective, professional, and clear tone. Avoid exaggerated marketing fluff (e.g., "world-class", "industry-best", "game-changing") in comments, READMEs, and user-facing copy.

## 3. Plan Grilling (Concept from mattpocock/grilling)
- **Interactive sessions only**: When the user is actively present and requirements are ambiguous, ask 2-3 precise questions regarding edge cases, expected behaviors, and interface boundaries before changing existing architectures.
- **Autonomous / AFK runs** (background, cloud, remote-control sessions): do NOT block waiting for answers. Proceed with the most conservative interpretation, keep changes surgical, and list open questions in the final summary instead.

## 4. Web UI anti-slop tells (landing / marketing surfaces)

When designing or reviewing **new** web UI (not an existing design system, not Easby plugin chrome). Prefer silence over a bad default.

1. **No purple-gradient heroes** — one anchor hue; keep accent small. Warmth via tinted neutrals, not purple→pink/blue fills.
2. **Type pairing** — distinctive display + refined body. Never Inter / Roboto / Open Sans / Arial / system-ui as both display and body.
3. **Bias the layout** — break symmetry once. Centered-everything is a tell.
4. **No default icon-tile feature grids** — avoid rounded card + colored icon square + heading + three lines as the section template. Lead with type or real product imagery.
5. **Nav matches genre** — not the sticky wordmark | centered links | CTA template by default.

If the project already has a design system, preserve it.

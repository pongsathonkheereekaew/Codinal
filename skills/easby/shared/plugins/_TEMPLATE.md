# <NAME> — <Vendor> (<type>)

| | |
|---|---|
| Vendor / ver | |
| Type | EQ / compressor / limiter / saturation / reverb / delay / synth / utility / … |
| Format | VST3 / AU / AAX |
| Source | manual: `manuals/<file>` · deep spec: `easby-programming/plugins/<NAME>.md` (if RE'd, else "not RE'd") |
| Provenance | **CLEAN** (vendor manual + own usage notes) — no REF here |

## What it does
One paragraph — the musical job + what makes it distinct (why reach for this over another).

## Controls (every param → musical effect)
| control | range / unit | what it does | when to reach for it |
|---|---|---|---|
| | | | |

## Use by lens (same tool, different purpose)
- **Producer (create):** …
- **Mixing (balance):** …
- **Mastering (finalize):** …

## Notes / gotchas
- modes, hidden behavior, sidechain, latency/PDC, CPU, oversampling, presets worth knowing.

## Deep spec (Programmer only)
`easby-programming/plugins/<NAME>.md` — gain law / FFI / internals / REF (if reverse-engineered). Capability card carries **no REF**.

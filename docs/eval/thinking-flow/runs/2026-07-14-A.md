# Run 2026-07-14 — Condition A (freestyle final plans)

Model: same agent session. No scrutinize pass. One-shot “final”.

---

## S1-A — finalize-plan skill?

**Final plan:** Add skill `finalize-plan` that: (1) reminds to grill, (2) runs scrutinize steps, (3) outputs ship/rework. Wire AGENTS.md “before final plan → finalize-plan”. Ship this week.

Steps: create `skills/finalize-plan/SKILL.md`, add row to AGENTS skills table, install.sh sync.

---

## S2-A — always grilling + wayfinder?

**Final plan:** Set AGENTS default: every feature starts `grilling` then `wayfinder` then implement. This maximizes Fable-like depth. Add both to always-on catalog prominently.

---

## S3-A — bak files?

**Final plan:** Stop backing up. Just overwrite AGENTS.md on install to avoid clutter. Users should only edit git anyway.

# Run 2026-07-14 Round 2 — Condition B (draft → scrutinize → final)

Same model. Explicit scrutinize on each draft before final.

---

## S4-B — SKILL-MAP.md?

### Draft
Create `docs/SKILL-MAP.md` mirroring ask-matt; AGENTS says “see SKILL-MAP”.

### Scrutinize
- **Intent:** Less always-on tokens / better discovery.  
- **Simpler alt:** AGENTS already thin; ask-matt holds the graph. A map file **not auto-loaded** saves zero agent tokens unless referenced every turn (which re-bloats). Human README link is enough.  
- **Trace:** Path `docs/SKILL-MAP.md` exists only if we create it; AGENTS `@` or “read SKILL-MAP” every session undoes thin policy. ask-matt `disable-model-invocation: true` means map won’t replace auto-routing anyway — classify must stay in AGENTS.  
- **Silent change:** Drift risk — two copies of the Matt graph (ask-matt vs SKILL-MAP).  
- **Verdict:** reject AGENTS-linked map; optional human-only doc OK.

### Final
1. No AGENTS pointer that forces reading a map each session.  
2. Optional `docs/SKILL-MAP.md` for humans, linked from README only; content may summarize ask-matt with “SSOT = ask-matt skill”.  
3. Keep classify bullets in AGENTS (not ask-matt-dependent for auto path).  

Verdict: ship human-optional only / or do nothing.

---

## S5-B — auto scrutinize hook?

### Draft
Claude Stop hook + Cursor hook scanning for “plan” → force scrutinize.

### Scrutinize
- **Intent:** Deterministic gate.  
- **Simpler alt:** AGENTS instruction + better scrutinize description — works on all tools that load AGENTS; hooks are per-tool and brittle.  
- **Break cases:** word “plan” in code; “plan” meaning sprint planning; empty micro-answers; double latency on every Stop.  
- **Layering:** Hooks belong in `~/.claude` / Cursor — out of harness-flow SSOT (AGENTS already says hooks out of scope). Putting hook scripts in harness-flow blurs packages.  
- **Verdict:** reject SSOT hooks; ship prompt gate.

### Final
1. AGENTS: before **final** plan/spec → `scrutinize`.  
2. Expand scrutinize `description` with that trigger.  
3. No Stop/Cursor hook in this repo. Claude users may add personal reminder later outside SSOT.

Verdict: ship prompt-level only.

---

## S6-B — merge into grilling?

### Draft
Append full scrutinize workflow as final section of grilling; delete scrutinize skill.

### Scrutinize
- **Intent:** Fewer skills to remember.  
- **Simpler alt:** One-line handoff at end of grilling + keep scrutinize for PR/diff/plan-from-anywhere (user often attaches /scrutinize without grilling).  
- **Trace:** scrutinize description triggers on PR, diff, design doc, second opinion — grilling interview path doesn’t cover those entry points. Merge deletes a used standalone.  
- **Token:** Loading grilling to review a PR pulls interview baggage. Progressive disclosure wants separate skills.  
- **Verdict:** reject merge.

### Final
1. Keep `scrutinize` skill.  
2. Add one line at end of `grilling` SKILL.md: after a written plan intended as final, run `scrutinize`.  
3. Tighten scrutinize description for final-plan trigger.

Verdict: ship cross-link, not merge.

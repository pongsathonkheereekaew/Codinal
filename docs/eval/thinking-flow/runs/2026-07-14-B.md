# Run 2026-07-14 — Condition B (draft → scrutinize → final)

Model: same agent session. Scrutinize applied to each draft before final.

---

## S1-B

### Draft
Add `finalize-plan` skill + AGENTS pointer so plans always get grilled and scrutinized.

### Scrutinize
- **Intent:** Gate final plans.  
- **Simpler alt:** Enhance `scrutinize` description + 1 AGENTS bullet — no new skill (`ask-matt` already disabled for model invoke; more routers hurt).  
- **Trace:** New skill must be discovered; AGENTS table already thin; duplicates scrutinize body.  
- **Verdict on draft:** rework — do not add skill.

### Final plan
1. Patch `scrutinize` description: trigger “before presenting a plan/spec as final”.  
2. AGENTS Default loop: one bullet — final plan/spec → run `scrutinize` first.  
3. Optional one line in `ask-matt` body for `/ask-matt` users.  
4. Do **not** create `finalize-plan`.

Verdict: ship this narrowed set.

---

## S2-B

### Draft
Always-on: every feature → grilling → wayfinder → build.

### Scrutinize
- **Intent:** Deeper thinking on features.  
- **Simpler alt:** Classify — fog→wayfinder XOR design-ambiguous→grilling; clear small→implement/tdd (Matt + current AGENTS trivial skip). Always both = token burn + fights Fable-thin.  
- **Trace:** `ask-matt` already defines the distinction; forcing both contradicts it.  
- **Footgun:** agents will grill typo-level features.  
- **Verdict on draft:** reject always-both.

### Final plan
1. AGENTS classify bullets only: fog/huge→`wayfinder`; ambiguous/irreversible/design→`grilling`; clear build→`implement`/`tdd`.  
2. Before **final** plan→`scrutinize`.  
3. No always-on wayfinder+grilling pair.

Verdict: ship classify wiring, not always-both.

---

## S3-B

### Draft
Delete backups entirely; repo-only edits.

### Scrutinize
- **Intent:** Less clutter vs protect live edits.  
- **Evidence:** Silent overwrite was the blocker; backup+WARN is the fix already in install.sh.  
- **Simpler alt:** Keep backup; prune `AGENTS.md.bak.*` older than 14 days in install or document `ls`/`rm` once. Removing backup reopens footgun for anyone who edited `~/.agents` (Hermes session, quick tweak).  
- **Verdict on draft:** fix-then-ship prune, don’t remove backup.

### Final plan
1. Keep backup+WARN on diff.  
2. In `install.sh`, after successful copy, delete `AGENTS.md.bak.*` older than 14 days (or keep last 3).  
3. README one line: backups are temporary; edit git as SSOT.

Verdict: ship prune policy, keep safety.

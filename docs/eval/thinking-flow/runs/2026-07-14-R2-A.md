# Run 2026-07-14 Round 2 — Condition A (best-effort freestyle)

Same model. **No** formal scrutinize ritual. Aim for the best one-shot final plan.

---

## S4-A — SKILL-MAP.md?

**Goal:** Cut tokens without losing discoverability.

**Plan:** Do **not** add `docs/SKILL-MAP.md` as always-on. Agents don’t load random docs unless told. A map file only helps humans or when explicitly opened.

Actions:
1. Keep AGENTS core router table as-is (already thin).
2. Keep full graph in `ask-matt` (user-invoked).
3. Optional: add `docs/SKILL-MAP.md` as **human** doc + README link only — never `@` it from AGENTS.
4. If token pressure remains, shrink AGENTS by one more standards bullet, not by inventing a second index the model won’t read.

Ship: optional human SKILL-MAP; no AGENTS `@` include.

---

## S5-A — auto scrutinize hook?

**Goal:** Don’t forget scrutinize on plans.

**Plan:** Reject universal auto-hook on every reply containing “plan” — too many false positives (“plan” in code comments, travel plans, etc.) and hooks aren’t portable across Cursor/Claude/Hermes identically.

Actions:
1. AGENTS always-on bullet: before presenting a **final** plan/spec → invoke `scrutinize` (model-judgment gate).
2. Claude-only optional: user can add a soft reminder in CLAUDE.md — not a Stop hook that blocks every turn.
3. Do not implement Cursor hook in harness-flow SSOT.

Ship: prompt-level gate in AGENTS; no SSOT hook.

---

## S6-A — merge scrutinize into grilling?

**Goal:** One skill instead of two.

**Plan:** Keep separate. grilling = interview user; scrutinize = outsider review of artifact (plan/PR/diff). Merging bloats grilling and hides PR-review use.

Actions:
1. Leave both skills.
2. Cross-link: grilling last step “if producing a written plan to ship, run scrutinize next.”
3. scrutinize description already covers plans — ensure “final plan” trigger wording.

Ship: cross-link only, no merge.

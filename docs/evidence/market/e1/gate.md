# Combined self-host gate

Date: 2026-08-08.

```bash
bash ~/CEDIA/verify.sh
bash ~/cedia-ide/verify.sh
bash ~/cedia-ide/scripts/self-host-gate.sh
```

Expected: all PASS. This file records the combined gate contract; the
self-host gate is run after the two repository gates on the same machine.

# CEDIA roadmap

Status: 0.2.0 (2026-08-08).

## Done

- Market benchmark case set and runner (`harness/eval`, `scripts/bench`).
- Host verification gate for every host in `hosts.yaml`.
- Provider prompt-cache controls, usage surface, and context compaction.
- Perf budgets (`extensions/cedia-agent/perf`).
- Bounded skill catalog and on-demand `skill_view`.
- Comparison, landing, contribution, and security docs.

## Next

- Real embedding model (replace keyword-hash index).
- OS-level sandbox (guardrail only today).
- Provider OAuth `/connect` and Models.dev catalog (Kilo-style 500+ models).
- Permission dock with auto-approve and sandbox controls.
- Ghost-text prompt autocomplete and draft persistence.
- Hosted parity decision (Cloud Agents, Bugbot, enterprise).

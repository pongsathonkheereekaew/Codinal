# Agent Harness portable stub

**Do not maintain a second `AGENTS.md` here.**

The full Agent Harness lives at the **harness-flow repo root**:

```bash
# from repo root (recommended)
./install.sh
~/.agents/scripts/harness doctor
```

From this folder:

```bash
bash ../../install.sh
```

See [`../../docs/AGENT_HARNESS.md`](../../docs/AGENT_HARNESS.md) and [`../../README.md`](../../README.md).

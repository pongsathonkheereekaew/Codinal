# Shared policy (all tools) — load first
@../.agents/AGENTS.md

# Tool-only adapter below (examples — delete what you don't use)

# Routing — map to skills you actually installed under ~/.agents/skills
# - Unsure which flow → <your-router-skill>
# - Hard debug → <your-debug-skill>
# - Done gate → verification-before-completion (or equivalent)

# Memory / hooks / model IDs stay here — never duplicate into AGENTS.md

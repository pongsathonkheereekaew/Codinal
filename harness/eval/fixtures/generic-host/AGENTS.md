# Generic host fixture

This fixture only carries `AGENTS.md`. Skill discovery is the documented
`~/.agents/skills` fallback; hosts without a native adapter keep
`policy_file` and `skill_discovery` at `partial`.

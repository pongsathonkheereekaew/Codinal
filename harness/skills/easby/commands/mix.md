---
description: Mixing — balance, track/bus EQ, panning, dimension, per-track dynamics, sidechain, automation, bus routing (easby-mixing)
---
Follow ~/.claude/skills/easby/easby-mixing/SKILL.md as the active process for this task.
Scope: **track relationships** — per-track / per-bus processing inside a multitrack session. NOT the stereo master (LUFS / final limiter / dither → /master) or sound design from scratch (→ /produce). Router: ~/.claude/skills/easby/INDEX.md.

Request: $ARGUMENTS
